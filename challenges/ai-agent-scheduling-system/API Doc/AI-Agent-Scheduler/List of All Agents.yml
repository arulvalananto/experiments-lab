info:
  name: List of All Agents
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/agents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
